# OneNight - Быстрый старт

## ✅ Что сделано

Ваше приложение теперь использует **Firebase Firestore** вместо localStorage. Все данные синхронизируются между всеми пользователями в реальном времени!

### Изменения:
- ✅ Модели сохраняются в Firebase
- ✅ Заказы сохраняются в Firebase  
- ✅ Настройки сохраняются в Firebase
- ✅ Автоматическая синхронизация в реальном времени
- ✅ Данные доступны на всех устройствах

## 🚀 Быстрая настройка (5 минут)

### 1. Создайте проект Firebase

1. Откройте https://console.firebase.google.com/
2. Нажмите "Добавить проект"
3. Введите название (например, "onenight")
4. Создайте проект

### 2. Включите Firestore

1. В меню слева выберите "Firestore Database"
2. Нажмите "Создать базу данных"
3. Выберите **"Тестовый режим"** (для начала)
4. Выберите регион (europe-west1)
5. Нажмите "Включить"

### 3. Получите конфигурацию

1. В настройках проекта (⚙️) выберите "Настройки проекта"
2. Прокрутите вниз до "Ваши приложения"
3. Нажмите на иконку веб-приложения `</>`
4. Зарегистрируйте приложение
5. Скопируйте `firebaseConfig`

### 4. Обновите конфигурацию

Откройте файл `src/firebase/config.ts` и вставьте свои данные:

```typescript
const firebaseConfig = {
  apiKey: "ВАШ_API_KEY",
  authDomain: "ВАШ_PROJECT_ID.firebaseapp.com",
  projectId: "ВАШ_PROJECT_ID",
  storageBucket: "ВАШ_PROJECT_ID.appspot.com",
  messagingSenderId: "ВАШ_MESSAGING_SENDER_ID",
  appId: "ВАШ_APP_ID"
}
```

### 5. Запустите приложение

```bash
npm run dev
```

Готово! 🎉

## 📝 Важные правила безопасности

Для продакшена обновите правила Firestore:

1. В Firebase Console → Firestore Database → Правила
2. Вставьте:

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /models/{modelId} {
      allow read: if true;
      allow write: if request.auth != null;
    }
    match /orders/{orderId} {
      allow read, write: if request.auth != null;
    }
    match /settings/{settingId} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
```

## 🔐 Добавить аутентификацию (рекомендуется)

1. Firebase Console → Authentication → "Начать"
2. Включите "Email/Password"
3. Добавьте страницу логина в приложение

## 📊 Структура базы данных

```
firestore/
├── models/          # Пользовательские модели
│   ├── {id}
│   │   ├── name
│   │   ├── age
│   │   ├── location
│   │   └── ...
├── orders/          # Заказы
│   ├── {id}
│   │   ├── modelId
│   │   ├── modelName
│   │   ├── date
│   │   └── ...
└── settings/        # Настройки приложения
    └── {id}
        └── telegramSupport
```

## 🆘 Проблемы?

### Ошибка: "Firebase: Error (auth/api-key-not-valid)"
→ Проверьте правильность API ключа в `config.ts`

### Данные не сохраняются
→ Проверьте правила безопасности в Firestore

### Ошибка в консоли браузера
→ Откройте F12 и проверьте детали ошибки

## 📚 Дополнительная информация

Подробная инструкция: `FIREBASE_SETUP.md`

## 🎯 Что дальше?

- [ ] Добавить Firebase Authentication
- [ ] Настроить правила безопасности для продакшена
- [ ] Добавить загрузку изображений в Firebase Storage
- [ ] Настроить индексы для быстрых запросов
